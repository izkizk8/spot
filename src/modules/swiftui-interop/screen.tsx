/**
 * @file screen.tsx
 * @description SwiftUI Interop screen - iOS default (placeholder for T005)
 */

import React from 'react';
import { View } from 'react-native';

export default function SwiftUIInteropScreen() {
  return <View testID="swiftui-interop-screen" />;
}
