/**
 * Local Authentication — Android screen (feature 022).
 *
 * The underlying expo-local-authentication API supports Android natively
 * (Fingerprint / Face / Iris on supported devices). The Android variant is
 * therefore identical to the iOS variant.
 */

export { default } from './screen';
