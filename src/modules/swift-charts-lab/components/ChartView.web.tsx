/**
 * T026: ChartView.web.tsx — Web fallback
 *
 * Identical to android variant. LayoutAnimation is a no-op on web (acceptable per spec).
 */

export { ChartView, type ChartViewProps } from './ChartView.android';
